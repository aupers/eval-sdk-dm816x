config FS_APPS_LIBUSB_COMPAT
	bool "libusb-compat-0.1.3"
	select FS_APPS_LIBUSB
	help
	libusb-compat is a replacement for libusb-0.1. 
	However, instead of being an actual implementation, 
	libusb-0.1 is more of a compatibility layer (or wrapper) which 
	just converts libusb-0.1 calls into their libusb-1.0 equivalents